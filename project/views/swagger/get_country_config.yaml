summary: "Get Country"
description: "This is a simple API which returns country"
consumes:
- "application/json"
produces:
- "application/json"
parameters:
- in: "path"
  name: "country_id"
  type: "integer"
  required: true
responses:
  200:
    description: "Country"
  400:
    description: "Bad request"
  404:
    description: "Invalid input"