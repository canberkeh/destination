tags:
- CITY
summary: "Get Cities"
description: "This is a simple API which returns cities by country_id"
consumes:
- "application/json"
produces:
- "application/json"
parameters:
- in: "path"
  name: "country_id"
  type: "integer"
  required: true
responses:
  200:
    description: "Country"
  400:
    description: "Bad request"
  404:
    description: "Invalid input"