summary: "Get Cities"
description: "This is a simple API which returns cities"
consumes:
- "application/json"
produces:
- "application/json"
parameters:
- in: "path"
  name: "country_id"
  type: "integer"
  required: true
responses:
  200:
    description: "Cities"
  500:
    description: "Invalid input"